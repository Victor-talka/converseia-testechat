# Exemplo de variáveis de ambiente para o Widget Converser

# URL base da aplicação (será automaticamente definida pelo Vercel)
# VITE_APP_URL=https://seu-dominio.vercel.app

# Exemplo de variáveis de ambiente para o Widget Converser

# URL base da aplicação (será automaticamente definida pelo Vercel)
# VITE_APP_URL=https://seu-dominio.vercel.app

# Configurações do Baserow - OPCIONAIS (se não configuradas, usará localStorage)
VITE_BASEROW_API_TOKEN=wT4NNP5hwTaVuzixirWycVT4D4xDRorE
VITE_BASEROW_BASE_URL=https://api.baserow.io/api
VITE_BASEROW_DATABASE_ID=296836
VITE_BASEROW_CLIENTS_TABLE_ID=689319
VITE_BASEROW_SCRIPTS_TABLE_ID=689333

# Configurações de CORS (já configuradas no vercel.json)
# Adicione aqui outras variáveis que possam ser necessárias

# IMPORTANTE: Se as variáveis do Baserow não estiverem configuradas,
# a aplicação funcionará normalmente usando localStorage como storage.
# Isso permite usar a aplicação sem configurar Baserow inicialmente.

# Para ativar Baserow (opcional):
# 1. Acesse https://baserow.io e crie uma conta
# 2. Crie um novo banco de dados
# 3. Crie duas tabelas com os seguintes campos:
#
# Tabela "Clientes":
# - name (Text) - Nome do cliente
# - email (Text) - Email do cliente  
# - company (Text) - Empresa do cliente
#
# Tabela "Scripts":
# - client_id (Number) - ID do cliente
# - client_name (Text) - Nome do cliente
# - script (Long Text) - Script do chatbot
# - title (Text) - Título do script
# - is_active (Boolean) - Se o script está ativo
#
# 4. Obtenha o API Token em Account Settings > API Tokens
# 5. Obtenha os IDs do banco e tabelas nas URLs do Baserow
# 6. Configure as variáveis acima
# 7. Redeploy a aplicação

# Configurações de CORS (já configuradas no vercel.json)
# Adicione aqui outras variáveis que possam ser necessárias

# Para criar um projeto Firebase:
# 1. Acesse https://console.firebase.google.com/
# 2. Crie um novo projeto
# 3. Ative o Firestore Database
# 4. Configure as regras de segurança (para desenvolvimento):
#    rules_version = '2';
#    service cloud.firestore {
#      match /databases/{database}/documents {
#        match /{document=**} {
#          allow read, write: if true;
#        }
#      }
#    }
# 5. Copie as configurações do seu projeto para as variáveis acima