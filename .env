# Variáveis de ambiente para o Widget Converser

# URL base da aplicação (será automaticamente definida pelo Vercel)
# VITE_APP_URL=https://seu-dominio.vercel.app

# Configurações do Firebase - OPCIONAIS (se não configuradas, usará localStorage)
# VITE_FIREBASE_API_KEY=your-api-key-here
# VITE_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
# VITE_FIREBASE_PROJECT_ID=your-project-id
# VITE_FIREBASE_STORAGE_BUCKET=your-project.appspot.com
# VITE_FIREBASE_MESSAGING_SENDER_ID=123456789
# VITE_FIREBASE_APP_ID=1:123456789:web:abcdef123456

# Configurações de CORS (já configuradas no vercel.json)
# Adicione aqui outras variáveis que possam ser necessárias

# IMPORTANTE: Se as variáveis do Firebase não estiverem configuradas,
# a aplicação funcionará normalmente usando localStorage como storage.
# Isso permite usar a aplicação sem configurar Firebase inicialmente.

# Para ativar Firebase (opcional):
# 1. Acesse https://console.firebase.google.com/
# 2. Crie um novo projeto
# 3. Ative o Firestore Database
# 4. Configure as regras de segurança (para desenvolvimento):
#    rules_version = '2';
#    service cloud.firestore {
#      match /databases/{database}/documents {
#        match /{document=**} {
#          allow read, write: if true;
#        }
#      }
#    }
# 5. Descomente e configure as variáveis acima
# 6. Redeploy a aplicação